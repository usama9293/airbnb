<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Listings</title>
</head>
<body>
    <div>
        <ul>
            <li>
                <h2>Listings details</h2>
            </li>
            <li>
                <%= listing.title %>
            </li>
            <!-- <li>
                <img src="<%= listing.image.url %>" alt="list image"/>
            </li> -->
            <li>
                <%= listing.description %>
            </li>
            <li>
             <%= listing.price.toLocaleString("en-pk") %>
            </li>
            <li>
                <%= listing.location %>
            </li>
            <li>
                <%= listing.country %>
            </li>
        </ul>
        <br>
        <a href="/listings/<%= listing._id %>/edit" >edit this listing</a>
    </div>
</body>
</html>